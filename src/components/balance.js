import React from "react";
import {Box, Typography} from "@mui/material"

const Balance = () => {
    return(<Box><Typography variant="h5" fontWeight="500">Current Balance: </Typography><Typography>$0.0</Typography></Box>)
}

export default Balance;